@model IList<ProductTagModel>

@if (Model.Count > 0)
{
    <div class="block block-popular-tags">
        <div class="tags">
            <div class="row">
                <div class="col-12">
                    <div class="nav nav-pills row li">
                        @for (var i = 0; i < Model.Count; i++)
                        {
                            <div class="col-auto mt-3">
                                <a href="@Url.RouteUrl("ProductsByTag", new { Model[i].SeName })" class="redtag">
                                    @Model[i].Name<span> @T("Products.Tags.Count", Model[i].ProductCount)</span>
                                </a>

                            </div>
                        }
                    </div>
                </div>
            </div>
            @*<ul class="nav nav-pills">
                @for (var i = 0; i < Model.Count; i++)
                {
                    <li class="">
                        <a href="@Url.RouteUrl("ProductsByTag", new { Model[i].SeName })" class="redtag">
                            @Model[i].Name<span> @T("Products.Tags.Count", Model[i].ProductCount)</span>
                        </a>

                    </li>
                    if (i != Model.Count - 1)
                    {
                        <li class="separator"> </li>
                    }
                }
                @for (var i = 0; i < Model.Count; i++)
                {
                    <li class="">
                        <a href="@Url.RouteUrl("ProductsByTag", new { Model[i].SeName })" class="redtag">
                            @Model[i].Name<span> @T("Products.Tags.Count", Model[i].ProductCount)</span>
                        </a>

                    </li>
                    if (i != Model.Count - 1)
                    {
                        <li class="separator"> </li>
                    }
                }
            </ul>*@
        </div>
    </div>

    @*<div class="product-tags-box">
            <div class="product-tags-list">
                <ul class="nav nav-pills">
                    @for (var i = 0; i < Model.Count; i++)
                    {
                        <li class="label label-default">
                            <a href="@Url.RouteUrl("ProductsByTag", new { Model[i].SeName })" class="label producttag">
                                @Model[i].Name<span> @T("Products.Tags.Count", Model[i].ProductCount)</span>
                            </a>

                        </li>
                        if (i != Model.Count - 1)
                        {
                            <li class="separator">, </li>
                        }
                    }
                </ul>
            </div>
        </div>*@
}
